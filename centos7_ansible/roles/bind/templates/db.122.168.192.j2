$TTL 84600
@       IN      SOA     ns.{{ bind_deploy_zone_name }}.   admin.{{ bind_deploy_zone_name }}. (
                        2016052601      ; Serial
                        10800           ; Refresh 3 hours
                        3600            ; retry 1 hour
                        604800          ; expire 1 week
                        600 )           ; min TTL 1 day

		IN	NS	ns.{{ bind_deploy_zone_name }}.

1		IN	PTR	ns.{{ bind_deploy_zone_name }}.
